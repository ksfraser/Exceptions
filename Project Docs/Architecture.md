# Architecture

## Overview

- Purpose of the package:
- Key modules/components:
  - `IException`: Interface for all custom exceptions.
  - `CustomException`: Abstract base class implementing `IException` and extending standard `Exception`.
  - Specific Exception Classes: Individual classes for each `KSF_` error code (e.g., `FieldNotSetException`, `InvalidDataValueException`).
- Primary data flows:

## Dependencies

- Direct dependencies:
- Optional/dev dependencies:
- Version constraints / PHP compatibility notes:

## Key decisions

- Link to entries in Decision_Log.md

